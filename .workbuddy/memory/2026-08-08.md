# 2026-08-08 GrayMatch 工作日志

1. 响应「改成不需要形状匹配的」：
   - 检查确认原生 C++ 已恢复为灰度 NCC（gray_model_native.cpp/h 无 matchMode/gradientMagnitude）。
   - 全仓库扫描仅发现 `GrayMatch.Wpf/obj2/` 下过期生成文件仍含 `ChkShapeMode`，确认是 CS0102 重复成员根因。
2. 清理：
   - 删除所有 `obj2`/`bin2` 目录（MSBuild 默认不排除它们）。
   - 清理上次遗留的临时脚本/tar 文件。
   - 默认 `obj`/`bin` 受沙箱删除钩子影响，部分被锁定未能清理，但源码编译已通过。
3. 构建验证：
   - `dotnet build GrayMatch.slnx`：3 个项目均成功编译，0 警告。
   - 沙箱钩子导致 WPF 的 `obj/.../FileListAbsolute.txt` 写入被拒（MSB3491），与代码无关。
   - 手动将 `GrayModelNative.dll` + `opencv_world480.dll` 拷入 WPF 输出目录后，`GrayMatch.Wpf.exe` 成功启动（PID 1068）。
4. 测试：
   - `dotnet test GrayMatch.Tests`：3/3 通过。
   - 在 `C:\gmrun5\Demo` 用当前 RotatedTemplateMatcher.cs/MatchResult.cs 跑端到端灰度 NCC demo：4 个旋转目标（0°/35°/118°/250°）全检出，中心误差 <2 px，角度误差 <=2°，内核耗时 ~19 ms，结果图保存为 `C:\Users\admin\WorkBuddy\graymatch_result.png`。
