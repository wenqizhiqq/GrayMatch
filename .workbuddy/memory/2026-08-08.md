# 2026-08-08

## 形状匹配功能运行验证

- 用户截图显示 VS 内 WPF 报 CS0102 重复定义；定位根因为工作区残留 `obj`/`bin`/`obj2`/`bin2` 生成目录被 MSBuild 默认 glob 重复编译。
- 将工作区源码干净拷贝到 `C:\gmrun3`（排除 `obj*`、`bin*`、`.git`、`ext`），并拷入预编译的 `GrayModelNative.dll` + `opencv_world480.dll`。
- 在 `C:\gmrun3` 执行 `dotnet build GrayMatch.slnx -c Debug`：
  - 0 警告，0 错误，WPF / WinForms / Tests 全部编译成功。
- 执行 `dotnet test GrayMatch.Tests/GrayMatch.Tests.csproj -c Debug`：
  - 3/3 测试通过：`UnitTest1.Test1`、`Can_Detect_Rotated_Patterns`、`Benchmark_All_Angle_Sweep`。
- 左侧「形状匹配（边缘）」复选框已存在，`IsShapeMode` 绑定与 `_matcher.MatchMode` 传参正确。
- 给用户指令：关闭 VS，删掉 `GrayMatch`/`GrayMatch.Wpf`/`GrayMatch.Tests` 下的 `obj`、`bin`、`obj2`、`bin2`，再 Rebuild 即可。

## 形状匹配原生崩溃修复（用户要求"运行看看"）
- 写了一个 Demo 控制台（`C:\gmrun3\Demo`）驱动两种模式。mode0 正常，mode1 进程 exit 127（无托管异常）→ 原生层访问违规。
- 加 `fprintf(stderr)` 调试定位：崩溃在 `cv::magnitude(gx, gy, mag)`，根因是 `gradientMagnitude()` 用 `cv::Sobel(..., CV_16S)`，`cv::magnitude` 在 opencv_world480(vc16) 上对 CV_16S 输入会崩。
- 修复：Sobel 改 `CV_32F`，`magnitude` 在 32F 正常，末段 `convertScaleAbs` 转 8U。已 `ninja` 重建 `GrayModelNative/build-ninja7/GrayModelNative.dll`。
- 复跑 Demo：mode0 4 结果/12.1ms；mode1 7 结果/17.7ms（场景1）；场景2 强椒盐噪声下各 1 结果。两模式均稳定跑通。
- 复跑 xUnit：3/3 仍 PASS。
- 结论：形状匹配功能已真正可运行；工作区原生 DLL 已含修复。
