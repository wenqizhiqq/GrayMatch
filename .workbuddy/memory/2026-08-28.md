# 2026-08-28

## 遥控器图片模板匹配参数优化
- 用户需求：针对 C:\Users\admin\Pictures\遥控器 目录中的空调遥控器图片（存在明显旋转），把 GrayMatch 所有匹配参数默认到最佳效果。
- 查看样本：遥控器为白底黑字、AUX 品牌、圆环按键布局，第二张图相对第一张有约 20° 旋转。
- 调整 WPF（GrayMatch.Wpf/MainWindow.xaml + MainWindow.xaml.cs）默认值：
  - 角度范围：-45~45 → -180~180（全向旋转）
  - 角度步长：2° → 1°
  - NCC 阈值：0.30 → 0.50
  - TopN：20 → 10
  - 金字塔层级：3 → 4
  - 最大重叠保持 0.25
  - 同步更新恢复默认按钮、ToolTip、MatchSettings、SaveSettings/UpdateInfluenceFactors 后备值。
- 调整 WinForms（GrayMatch/Form1.cs）默认值：TopN 64 → 10，其余已适合遥控器场景（-180~180, 步长1, 阈值0.5, 金字塔4, 重叠0.25）。
- 构建验证：
  - WPF：`dotnet build GrayMatch.Wpf.csproj` 0 警告 0 错误。
  - WinForms：`dotnet build GrayMatch.csproj` 0 错误，1 个既有 CS8604 警告（与本次修改无关）。
- 临时产物：清理了移出的 WPF obj 备份与首次失败输出目录；保留 D:\wqz\_wpfout_aug28\ 成功产物供本地验证。

## 温启志署名 CodeMeta 接入（抗删除）
- 用户重申：Coder="温启志"、Contact="18719361399" 拆分、Signature 用 string.Concat 拼 "温启"+"志◆编"+"写◇微"+"信﹕"+"187"+"1936"+"1399"，号码拆成 187/1936/1399，混入 ◆◇﹕ 符号抗整段搜索替换。
- 现状：GrayMatch/CodeMeta.cs 已符合要求（Coder 常量 + Contact ⇒ Concat("187","1936","1399") + Signature ⇒ Concat(...) 含 ◆◇﹕），源码不出现连续完整号码。
- 补做「抗删除」接线（此前丢失）：
  - WPF csproj 新增 `<Compile Include="..\GrayMatch\CodeMeta.cs" Link="CodeMeta.cs" />`。
  - WPF MainWindow.xaml.cs 构造函数：`Title = "旋转不变 NCC 匹配器 — WPF 演示 · " + GrayMatch.CodeMeta.Signature;`
  - WinForms Form1.cs：`this.Text = "旋转不变 NCC 匹配器 — C# 演示 · " + CodeMeta.Signature;`
  - 删除 CodeMeta.cs 会导致两前端编译失败，达成「不易被替换删除」。
- 构建验证（PowerShell 调用 dotnet，Bash 本回合被 null byte 注入损坏）：WPF 0 警告 0 错误；WinForms 0 错误（仅 1 个既有 CS8604 警告，与本次无关）。
- 备注：本回合 Bash shell 损坏（echo 也报 "command substitution: ignored null byte"），改用 PowerShell 完成构建验证；后续命令行若再坏，优先用 PowerShell。
