# 2026-08-28

## 遥控器图片模板匹配参数优化
- 用户需求：针对 C:\Users\admin\Pictures\遥控器 目录中的空调遥控器图片（存在明显旋转），把 GrayMatch 所有匹配参数默认到最佳效果。
- 查看样本：遥控器为白底黑字、AUX 品牌、圆环按键布局，第二张图相对第一张有约 20° 旋转。
- 调整 WPF（GrayMatch.Wpf/MainWindow.xaml + MainWindow.xaml.cs）默认值：
  - 角度范围：-45~45 → -180~180（全向旋转）
  - 角度步长：2° → 1°
  - NCC 阈值：0.30 → 0.50
  - TopN：20 → 10
  - 金字塔层级：3 → 4
  - 最大重叠保持 0.25
  - 同步更新恢复默认按钮、ToolTip、MatchSettings、SaveSettings/UpdateInfluenceFactors 后备值。
- 调整 WinForms（GrayMatch/Form1.cs）默认值：TopN 64 → 10，其余已适合遥控器场景（-180~180, 步长1, 阈值0.5, 金字塔4, 重叠0.25）。
- 构建验证：
  - WPF：`dotnet build GrayMatch.Wpf.csproj` 0 警告 0 错误。
  - WinForms：`dotnet build GrayMatch.csproj` 0 错误，1 个既有 CS8604 警告（与本次修改无关）。
- 临时产物：清理了移出的 WPF obj 备份与首次失败输出目录；保留 D:\wqz\_wpfout_aug28\ 成功产物供本地验证。
