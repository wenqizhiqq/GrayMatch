# 2026-08-17 工作记录

## 把「药名匹配配方」默认值写进 XAML（用户要求）
- 用户：把上轮定的「配方」参数直接写进 WPF 界面默认值。
- 配方 = 灰度模式（关轮廓）+ 147x502 药名竖条模板 + `Match(3,-45,45,2,0.30,0.25,20)`。
- `GrayMatch.Wpf/MainWindow.xaml` 改默认（6 个数值）：
  - `TbAngleStart` "-180"→"-45"；`TbAngleEnd` "180"→"45"；`TbAngleStep` "1"→"2"；
  - `TbThreshold` "0.5"→"0.30"；`TbOverlap` "0.1"→"0.25"；`TbTopN` "200"→"20"。
  - 金字塔 `CmbPyramid` SelectedIndex 已=3（代码按 ComboBoxItem 内容直接解析为 pyramidLevels=3，与配方一致，无需改）；`ChkContour` 已 IsChecked=False（灰度）；`ChkDense` False；Scale/Deform 滑块已=0。
- 代码级默认也同步：`MainWindow.xaml.cs` 的 `MatchSettings` 默认值改为 AngleStart=-45/End=45/Step=2/Threshold=0.30/Overlap=0.25/TopN=20（Overlap 本就是 0.25）。原因：这些参数会被序列化到 `settings.json`（`AppDataDir/settings.json`）并**启动时回填覆盖 XAML 默认值**——只改 XAML 不够，老用户机器上旧 settings.json 会盖掉。
- 构建验证：`dotnet build GrayMatch.Wpf -c Debug`（移开锁定 obj + `-p:BaseOutputPath` 重定向）**0 错误 3 警告**（NU1900 是沙箱 NuGet 漏洞库联网失败，无害）。
- 给用户提醒：若之前在本机跑过该 App，需删掉 `settings.json`（或手动在 UI 里把参数改一遍）才能用上新默认值；沙箱 push 仍 401，需用户 VS Rebuild + 本机验证。

## 用户反馈「匹配失败：Source and template must be set before matching」弹窗
- 根因：`RunMatchAsync` 里只判断了 `_matcher.Template == null`（提示「请先创建模板」），但没有判断 source 是否已加载。当用户点击「开始查找」时若还没打开图片（或图片加载失败），`_matcher.Match()` 会抛 `InvalidOperationException("Source and template must be set before matching.")`。
- 修复方案：
  1. `RotatedTemplateMatcher.cs` 增加 `public bool HasSource => _sourceGray != null;`。
  2. `MainWindow.xaml.cs` 的 `RunMatchAsync` 在模板判断前加 source 判空 guard，提示用户先打开图片。
- **沙箱限制**：当前 `GrayMatch.Wpf/MainWindow.xaml.cs` 与 `GrayMatch/RotatedTemplateMatcher.cs` 含 GBK 编码字节/被钩子部分损坏，`Edit` 无法匹配含中文区域，`Python`/`PowerShell` 写 `.cs` 文件被权限拒绝/静默失败，无法在此直接落盘 patch。已在沙箱内尝试多种方式均失败。
- 交付：给用户精确的 2 处 patch，请用户在本机干净环境下（VS）直接粘贴进去 Rebuild 即可。

## UI 调整：把「多尺度」滑块从轮廓参数区里挪出来
- 用户反馈：关轮廓后「模板大小倍数」仍影响灰度匹配，感觉轮廓参数被用到灰度里。
- 原因：`ScaleRange` 本质上是**全局参数**（`UseContour` 开关只决定用灰度图还是梯度图做匹配，多尺度对两者都生效），但 UI 上把它放在「轮廓匹配参数」折叠区里，容易误解。
- 改动：`MainWindow.xaml` 里把 `SldScaleRange`/`TbScaleRangeVal` 从轮廓 `Border` 内移出，单独放在轮廓区下方、标题改为「多尺度匹配（全局，灰度/轮廓均生效）」，永远可见。
- 构建验证：`dotnet build GrayMatch.Wpf -c Debug` **0 错误 3 警告**。
