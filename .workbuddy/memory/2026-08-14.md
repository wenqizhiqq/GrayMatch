# 2026-08-14

## 「其他电脑提示找不到 GrayModelNative.dll」根因与修复
- 根因：GrayModelNative.dll 依赖 **VC++ 2022 运行库**（dumpbin 实测导入 MSVCP140 / VCRUNTIME140 / VCRUNTIME140_1；opencv_world480.dll 还依赖 CONCRT140）。本机因装了 VS 有这些运行库；其他电脑没装 VC++ 可再发行包 → 加载 GrayModelNative.dll 失败，被报成「找不到 DLL」。
- 修复（两个 csproj 的 `CopyNativeDeps` 都改）：
  - 自动定位 VS 2022 Redist（`C:\Program Files\Microsoft Visual Studio\2022\{Community|Professional|Enterprise}\VC\Redist\MSVC` 或 BuildTools），用通配 `$(_VsRedistRoot)\*\x64\Microsoft.VC143.CRT\*.dll` + `...\Microsoft.VC143.OpenMP\vcomp140.dll` 把 VC 运行库打进输出目录（自包含，目标机无需装任何东西）。
  - 注意 MSBuild 坑：`Include` 通配**只匹配文件不匹配目录**，所以 `...\*\...\Microsoft.VC143.CRT`（以目录结尾）解析为空；必须在末尾带 `*.dll` 文件通配。
  - 同时把两个项目的 native DLL 统一指向 `build-ninja9`（WinForms csproj 之前还指向旧的 `build-ninja7`，那是 8 参数版，会与 C# 9 参数 gm_match 签名冲突 → 崩；已修正，并把 WinForms bin 里 160768 字节的旧 DLL 替换成 46080 的 ninja9）。
- 增强 `RotatedTemplateMatcher.Match()`：首个 native 调用包了 `DllNotFoundException`（中文提示缺哪些 DLL、要放一起拷贝）/ `BadImageFormatException`（提示 32/64 位不匹配、需 x64）。
- 验证：`dotnet build GrayMatch.Wpf ... /t:Rebuild` → 0 警告 0 错误；输出目录已含 GrayModelNative.dll、opencv_world480.dll、vcruntime140(_1/_threads).dll、msvcp140(_1/_2/_atomic_wait/_codecvt_ids).dll、concrt140.dll、vcomp140.dll。已同步到 WPF 默认 bin 与 WinForms bin。
- 部署须知：把**整个**输出文件夹（`bin\Debug\net8.0-windows` 或发布目录）一起拷到目标机运行；目标机需 64 位 Windows。

## 让 GrayModelNative 成为 VS2022 sln 内可编辑/编译的工程
- 之前 GrayModelNative 只有 CMake+Ninja 外部构建，不在 `GrayMatch.slnx` 里，VS 打开 sln 无法直接编辑/编译它。
- 新增 `GrayModelNative/GrayModelNative.vcxproj`（x64 DLL，C++17，定义 `GRAYMODEL_EXPORTS`，OpenCV include/lib 指向 `..\ext\opencv\opencv\build`，`/openmp`、`/utf-8`，输出直接到 `build-ninja9\` 让 C# CopyNativeDeps 无需改路径）。**关键**：Debug/Release 都强制 `RuntimeLibrary=MultiThreadedDLL (/MD)`，即链接发布版 VC 运行库，避免 Debug(/MDd) 版部署到无 VS 的电脑缺 `vcruntime140d` 再次“找不到 DLL”。
- `GrayMatch.slnx` 加入该 vcxproj；WPF 与 WinForms 的 csproj 增加 `ProjectReference`（`ReferenceOutputAssembly=false`、`UndefineProperties="Platform"`、`Condition="Exists('$(VCTargetsPath)')"`）——这样 VS 内 F5 会自动先编译 native，而 `dotnet build`(Core 无 VC 目标)会跳过，不破坏命令行构建。
- 验证：用 vcxproj 等效参数直接 `cl /LD ...` 编译出 DLL，dumpbin 确认 6 个 `gm_*` 导出齐全；WPF `dotnet build` 仍 0 警告 0 错误且 GrayModelNative.dll 正常拷贝。

## 精准裁剪 ext 文件夹（选项2）
- `ext` 957M → 73M（省 ~884M）。删除项：opencv-4.8.0-windows.exe(176M 安装包)、ninja/、sources(216M OpenCV C++ 源码)、build/java|python|etc、build/bin；`build/x64/vc16/bin` 仅留 `opencv_world480.dll`(删 PDB 285M/Debug dll 125M/视频IO dll/OpenCV 自带 exe 工具)，`build/x64/vc16/lib` 删 `opencv_world480d.lib`(debug 导入库)。
- **必留三件套**：`build/include/opencv2/opencv.hpp`(头文件)、`build/x64/vc16/lib/opencv_world480.lib`(链接)、`build/x64/vc16/bin/opencv_world480.dll`(运行时，nuget 不提供、native 与 OpenCvSharpExtern 都依赖)。
- vcxproj/csproj 无需改（引用的仍是这三条路径）。用裁剪后 ext 实编 gray_model_native.cpp 成功、6 个 gm_* 导出齐全 → 裁剪未破坏 native 构建。
- 注：删除经安全钩子（genie-trash）放行，曾有单文件回收站搬运报错但不影响实际删除。
