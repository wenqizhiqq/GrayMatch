# 2026-08-18

## 轮廓匹配漏检修复
- 用户反馈：勾选「轮廓匹配」后数字图中很多目标匹配不到（截图 14/18）。
- 根因：轮廓/边缘梯度图经高斯金字塔逐层下采样后细边被抹糊，粗层无法给出可靠种子，导致旋转目标漏检。
- 修复：RotatedTemplateMatcher.Match() 中当 UseContour 时强制 pyramidLevels = 0，走 legacy 两遍全分辨率路径。
- 附带修复：把 OpenCvSharp.Size.Zero 替换为 new OpenCvSharp.Size(0, 0)，避免 OpenCvSharp 4.13 下编译失败。
- 验证：控制台用 图片1300x1000.png + 70x104 模板测试：
  - 灰度：pyramid 0/3/5 均 18/18，min score ~0.94。
  - 轮廓（修复前）：pyramid=3/5 仅 24 个结果且多假阳性，60° 目标漏检。
  - 轮廓（修复后）：pyramid 0/3/5 统一走 legacy，不再丢高旋转目标；阈值 0.45 时可在 -5~65° 内干净检出全部 18 个。
- UI：更新 MainWindow.xaml 中「轮廓匹配」与「金字塔层级」ToolTip，提示轮廓模式忽略金字塔层级并建议阈值≥0.40。
- 构建：dotnet build GrayMatch.Wpf/GrayMatch.Wpf.csproj -c Debug -p:BaseOutputPath=_wpfout_contourfix2/ → 0 警告 0 错误。

## UI 美化（布局/配色/图标）
- 重写 GrayMatch.Wpf/MainWindow.xaml：现代化主题，统一调色板（主色 #2563EB / 深蓝 #1D4ED8 / 文字 #0F172A / 次 #64748B / 边框 #E2E8F0 / 背景 #F1F5F9）。
- 新增顶部蓝色标题栏（应用图标 + 标题 + 副标题 + “实时预览”徽标）。
- 左侧栏改为卡片式分区（使用说明 / 操作 / 匹配参数 / 轮廓参数 / 多尺度 / 缺陷检测 / 图像与运行状态），每张卡片白底圆角 + 轻投影。
- 全部矢量图标（DrawingImage/Path，无外部依赖）：帮助/文件夹/图片/裁剪/扫帚/刷新/滑块/轮廓/缩放/警告/芯片/信息/时钟/靶心。
- 按钮样式化：ModernButton（白底圆角描边）、PrimaryButton（蓝色实心“开始查找”）、LinkButton（图标式“恢复默认”）。
- 滑块重写 ModernSlider 控件模板：蓝色已选轨道 + 白色描边圆点 Thumb。
- 信息面板改为图标行 + 标签/值样式；缺陷统计改为可读红色；状态栏白底。
- 画布底色由纯黑改为 #0F172A（深蓝灰）更柔和。
- 构建：dotnet build GrayMatch.Wpf -c Debug -p:BaseOutputPath=_wpfout_beautify/ → 0 警告 0 错误。


## UI 美化（第二轮：全局主题 + 细节）
- 新建 GrayMatch.Wpf/App.xaml 全局主题（Application.Resources）：
  - 调色板 SolidColorBrush 资源（BrandPrimary/BrandPrimaryDark/BrandPrimaryLight/BrandAccent/TextStrong/TextMuted/LineBorder/Surface/SurfaceSoft/CanvasBg/Danger/ScrollThumb/ScrollThumbHover）+ BrandHeaderGradient 渐变。
  - 隐式样式（无 x:Key，全窗生效）：ListBox/ListBoxItem（圆角卡片、悬停 #EFF6FF、选中 #DBEAFE 蓝边）、ScrollBar（8px 圆角滑块、悬停变深、保留 PageUp/PageDown 点击）、CheckBox（自定义勾选方块，蓝色填充）。
- MainWindow.xaml 精修：
  - 顶部标题栏改为 #2563EB→#1E40AF 纵向渐变 + 投影（DropShadowEffect）增强层次。
  - 图片列表 LstImages 加 ItemTemplate：每条前面加图片小图标 + 文字省略号，配合全局 ListBox 样式高亮选中项。
  - 底部状态栏加信息图标 + 顶部细边框分隔。
- 说明：MainWindow 中显式 Style=ModernCheck 的 3 个复选框仍走原模板（无回归）；其余控件自动套用全局主题。
- 构建：dotnet build GrayMatch.Wpf -c Debug -p:BaseOutputPath=_wpfout_globaltheme/ → 0 警告 0 错误。
